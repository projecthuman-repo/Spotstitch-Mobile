import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    height: 48,
    width: 48,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 8,
  },
})
